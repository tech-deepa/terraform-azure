Terraform provider
----------------------------

Provider is the interface that interacts with the third party cloud provider apis.
As different cloud provides has different ways of interacting and sending data, for each cloud provider we have different providers.

Terraform file
--------------------

Terraform file will be created with .tf 
The steps to run tf file is -

Terraform init 
Terraform validate
Terraform paln
Terraform apply

Terraform checks the desired state with actual state when we run Terraform plan/apply.
It creates and deleted the resource to match the desired state.
If there is some modification in desired state, it can update/delete the infrastrcture.

--------------------------------
Statefile(remote backend)

Terraform statefile has details of the acutal state of the infrastrcture.
So that need to stored safely, so we are storing it in remote storage like s3 bucket or azure blob.
And we also need to maintain state locking, i.e we need to prevent people applying chaning simultaneously.
s3 bucket - dynamo db
azure blob by default support state locking.

-------------------------------------------------

Variables:
The variable is used to pass the required value at time of execution.
The variables can be passed from the .tfvars file, command line, ENV
There are precedenced for passing variable, when all are used.
1. Passing from command line (ex: Terraform paln -var variable_name = value )
2. Passing in the .tfvars (ex: Terraform plan -var-file = file.tfvars )
3. Environment variable (ex: export TF_VAR_variable = value)
4. default value   

Locals:
used for the paramter which need no to be changed or overriden

Output:
Used to print result needed after infrastrcture is created.

-----------------------------------------------------

Directory structure:

Here we create different file for different use cases.
provider.tf
backend.tf
variables.tf
output.tf 
ect..

Dependencies:
implicit Dependencies: where we reference the other resources
explict : where we use depends_on = [resource.name]

-----------------------------------------------

Type constratins:

string, number, bool

List : use to store the same type of value
variable var_name {
    type = list(string)
    default = ["item1","item2"]
}

accessing :
var.var_name[0] or var.var_name[1]

##############################
map: used to store in key value pair

variable var_name{
    type = map(string)
    default = {
        "Environment" = "dev"
        "owner" = "dee"
    }
}

accessing
var.var_name["Environment"]

##############################

tuple: list of different type 
variable var_name{
    type = tuple([string, string, number])
      description = "Network configuration (VNET address, subnet address, subnet mask)"
    default = ( ["10.0.0.1/16","10.0.1.0",24])
}

accessing:
element(var.var_name,0)

##############################

object: used to store the different set if values

variable var_name{
    type = object({
    size         = string
    publisher    = string
    offer        = string
    sku          = string
    version      = string
  })
}

accessing:
var.var_name.size or var.var_name.publisher

----------------------------------------------------

count when we need to create a multiple resources.
for_each need to be used for map, set. which has unqiur value

output "name"{
    value = [ for i in azurerm.storage_account.example: i.name]

}

>> If resources have names, IDs, or meaning → use for_each
>> If you only need N copies → use count

---------------------------------------------------------
life cycle 

we use lifecycle to handle the flow of life cycle

create before destroy: make sure the resource is created before deleting the existing resource
prevent destroy : prevent the deleltion of the resource
precondition: handles the value in the allow list
postcondition: check after the infra is created
ignore_changes: ignore the changed of these infrastrcture

--------------------------------------------------------

dynamic

for_each  → repeats RESOURCES
dynamic   → repeats BLOCKS inside resources

conditional expression

var.Environment == "dev" ? "dev_nsg" : "staging_nsg"

splat:

it is prefered when no tranformation is needed

aws_instance.web[*].public_ip  // splat expressionS

vs

[for i in aws_instance.web : i.public_ip]

-------------------------------------------------------------

functions:

used builtin fuction do perform some operations

-----------------------------------------------------

data source:

used to use the resource which already exists.
And that resource is not addeed to terraform state.
terraform doesnot take owner ship for the data source resource.

--------------------------------------------------------------

Problem faced:

Problem 1:

permission issue to run get providers when ran terraform plan:
>>Assign contributor and network administrator role to access the azure from terraform.
>>to create service principle we need user access administrator permission